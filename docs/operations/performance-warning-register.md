# Performance warning register

No open warnings at baseline creation. Add one row per P0/P1 signal from query contracts, controlled load, telemetry, DB or host monitoring. Valid closure states are `cleared` (re-measured below threshold) and `noise-dismissed` (repeat evidence attached); open exceptions are not allowed.
