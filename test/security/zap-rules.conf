# ZAP feedback-loop classification. Security findings remain WARN/FAIL by default.
# These two passive rules only classify expected response characteristics.
10049	IGNORE	(Cache behavior is intentional: nonce-bearing HTML is no-store; fingerprinted assets are immutable)
10109	IGNORE	(Modern web application detection is informational, not a vulnerability)
